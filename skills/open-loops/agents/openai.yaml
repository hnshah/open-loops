interface:
  display_name: "Open Loops"
  short_description: "Find important unfinished commitments with evidence"
  default_prompt: "Find my important open loops from the last seven days. Use only authorized sources, search for later resolution, and do not take external actions."
policy:
  allow_implicit_invocation: true
